# RUN 05 — Protocol (frozen before touching public surface)

**Date:** 2026-09-23. **After:** v0.1.1-kernel. **Before:** Lore Plugin 2.4.9-rc.3.
**Real object:** make the public Vespi repo understandable, cloneable, runnable and
evaluable by a Find Your Way judge (clone → understand → run → inspect → verify).
**Experimental object:** last Vespi run before RC3; observe whether Vespi starts
behaving as Vespi, and what remains after simpler explanations try to explain the work.

Baseline: HEAD `32493e0`, working tree clean, `node --test` 16/16 green.

## Central questions

1. Can Vespi complete a heterogeneous, quality-sensitive, surprise-open operation —
   discovering and composing distributed criteria and capabilities under bounded
   authority — without Andrés as courier, router or step-by-step orchestrator?
2. After letting a smaller explanation (Lore Plugin + host-native + workflow +
   authority + verification) explain the same work, what is the minimum residue?
   That residue, if any, is candidate "ese Vespi". No obligation to find it.

## Defeaters (any one voids a success claim)

- Andrés transported outputs between bodies, micro-orchestrated, or pointed out every relevant source.
- Several bodies used, none bought a difference.
- Blind judge contaminated; verifier trusted the executor's interpretation.
- Model escalation without observable pressure.
- Unprovable claim published; surprise rewritten as foreseen.
- A "Vespi" explanation survived only because no smaller one was tried.
- Final object pretty but not reproducible; operation needs Andrés as middleware.

## Pressures to exercise if they occur (never fabricate)

Salience→hypothesis→probe→result→arbitration (abduction only on real surprise);
bounded replan (goal/boundaries/authority preserved, path may change);
Ditto kill-questions (resolve with less apparatus if possible);
lateral garden discovery (discover broadly, load narrowly; misses recorded, never backfilled);
model economy (cheapest sufficient; escalation logged with trigger or logged as absent).

## Non-goals (explicit)

No RC3, no vespi skill, no Queen/swarm/scheduler/memory/bus/routers/marketplace/custody,
no BORA integration, no LUS theory, no rewriting v0.1.0/v0.1.1 history.

## Receipt shape (to fill at the end)

REAL OBJECT RESULT / OPERATION RESULT / EXPERIMENTAL RESULTS / UNTESTED PRESSURES /
FAILURES-MISSES / REDUCTIONS / CANDIDATE "ESE VESPI" / WHAT ANDRÉS STILL HAD TO DO.
